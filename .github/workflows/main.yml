name: Deploy Site on push
on:
  push:
    branches:
      - main
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get the latest code
      uses: actions/checkout@v2.3.2
    - uses: actions/setup-node@master
    - name: Installing project dependencies
      run: npm install
    - name: Building the project
      run: npm run --prod
    - name: 📂 Sync Files
      uses: SamKirkland/web-deploy@v1
      with:
        target-server: snowcloudmx.com
        remote-user: snowclou
        private-ssh-key: ${{ secrets.MYKEY }}
        ssh-port: 7822
        destination-path: ~/scedemo.snowcloudmx.com/
